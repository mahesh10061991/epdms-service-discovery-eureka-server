server:
    port: 8761
spring:
    application:
        name: epdms-service-discovery-eureka-server
eureka:
    instance:
        hostname: localhost
    client:
        register-with-eureka: false
        fetch-registry: false

